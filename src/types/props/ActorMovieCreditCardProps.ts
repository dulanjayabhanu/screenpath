import type {ActorMovie} from "@/types/ActorMovie.ts";
import type {Language} from "@/types/Language.ts";

export type ActorMovieCreditCardProps = {
    actorMovie: ActorMovie
    language: Language
}