import type {DirectorAndCrew} from "@/types/DirectorAndCrew.ts";

export type InternalDirectorAndCrewDataSortProps = {
    crew: DirectorAndCrew[]
    department: string
    genderSort: string
    sortBy: string
}