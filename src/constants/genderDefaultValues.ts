import type {GenderDefaultValues} from "@/types/GenderDefaultValues.ts";

const genderDefaultValues = (): GenderDefaultValues => {
    return {
        female: 1,
        male: 2,
    }
}

export default genderDefaultValues